keyness_dict = {}　#dictionary
ref_dict = {}		#dictionary
	
frec_dict1(reference corpus)
frec_dict2(target corpus)
frec_dict1 loop: number of word	i = 0 -> len(frec_dict1) text = frec_dict1[i]
if 		text not in ref_dect ref_dict[i] = 0

frec_dict2 loop: number of word	i = 0 -> len(frec_dict2) text = frec_dict2[i]
if 		text not in ref_dect ref_dict[i] = 0

loop: number of ref_dict i = 0 -> len(ref_dict) item = ref_dict[i]
if 		item not in frec_dict1 || frec_dict1[item] == 0　so frec_dict1[item] = small number	
#Avoid zeros to avoid interfering with calculations
if 		item not in rec_dict2 || frec_dict2[item] == 0　so frec_dict2[item] = small number	#Avoid zeros to avoid interfering with calculations

Calculate the percentage of KEYNESS.
return keyness_dict